import {
  TextInput
} from '@mantine/core'

import {
  useForm
} from '@mantine/form'

const General = () => {
  const form = useForm({
    mode: 'uncontrolled',
    initialValues: {
      name: '',
    }
  })

  return (
    <>
    </>
  )
}